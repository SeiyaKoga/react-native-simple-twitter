import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
    backgroundColor: 'rgb(35,188,168)'
  },
  button: {
    borderRadius: 2,
    backgroundColor: 'rgba(0, 0, 0, 0.15)',
    height: 45,
    width: 285,
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 20
  }
});

export default styles;
